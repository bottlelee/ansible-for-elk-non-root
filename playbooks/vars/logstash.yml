logstash_input_agent_gateway: "nginx.{{ elk_domain }}:{{ nginx_streams | selectattr('name', 'eq', 'elastic-agent') | map(attribute='listen_port') | first }}"
logstash_input_logstash_gateway: "nginx.{{ elk_domain }}:{{ nginx_streams | selectattr('name', 'eq', 'logstash-input') | map(attribute='listen_port') | first }}"